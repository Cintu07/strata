[package]
name = "strata-prefill"
description = "expert centric prefill scheduling: read each expert once per layer, not once per token"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
strata-format = { path = "../strata-format" }

[lints]
workspace = true
